# Krishti AI V15.2 — Google-Only Login

- Removed Email + Password login UI.
- Removed mobile/Phone OTP login UI and reCAPTCHA from the login screen.
- Kept Continue with Google as the only login method.
- Existing Firebase Google Authentication configuration remains unchanged.
- Admin/User dashboards and Firebase Admin server integration are preserved.
- Service worker cache version bumped to force the new login UI after deployment.

Firebase Google sign-in remains enabled in the Firebase project.
