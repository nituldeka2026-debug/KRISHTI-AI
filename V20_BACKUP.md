# V20 Backup Foundation

V20 keeps plan/usage/subscription records under `data/`. Back up `data/` before deployments or migrate these records to a managed database with scheduled backups. The existing Library export remains available for local chat backup.
